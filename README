Trabalho Sistemas Especialistas - Controle financeiro

Pequeno exemplo de aplicação:

    >>> from trabalho_1.bl import BancoLogico
    >>> bl = BancoLogico()
    >>> bl.quantia_poupada(22000)
    >>> bl.ganhos(25000, bl.ESTAVEL)
    >>> bl.dependentes(3)

    >>> bl.poupanca()
    True
    >>> bl.renda()
    False


TODO: passar este README para linguagem Markdown

